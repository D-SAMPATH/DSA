class Solution:
    def rotateArray(self, nums, k):
        n=len(nums)
        k = k % n
        temp=[]
        for i in range(0,k):
            temp.append(nums[i])
        for j in range(k,n):
            nums[j-k] = nums[j]
        for f in range(k-1,-1,-1):
            nums[n-k+f] = temp[f]
