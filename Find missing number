class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        n=len(nums)
        for i in range(0,n+1):
            flag = 0
            for j in nums :
                if i == j:
                    flag=1
                    break
            if flag == 0:
                return i
