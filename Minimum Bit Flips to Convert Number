class Solution:
    def minBitsFlip(self, start, goal):
        num = start ^ goal
        count = 0
        for i in range(32):
            count += (num & 1)
            num = num >> 1
        return count
