class Solution:
    def pascalTriangleII(self, r):
        ans = [0] * r 
        
        ans[0] = 1 
        
        for i in range(1, r):
            ans[i] = (ans[i-1] * (r - i)) // i
        
        return ans 
